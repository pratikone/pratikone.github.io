# frozen_string_literal: true
# do a 'bundle install'
# followed by 'jekyll build' and 'jekyll serve'

source "https://rubygems.org"

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# gem "rails"
gem "github-pages", "~> 219"
gem 'wdm', '>= 0.1.0' if Gem.win_platform?
gem "webrick", "~> 1.7"
